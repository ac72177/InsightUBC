{
    "title": "Invalid value type for audit in LT, should be number.",
    "query": {
        "WHERE": {
            "LT": {
                "courses_audit": "cpsc"
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "courses_dept",
                "courses_id",
                "courses_instructor",
                "courses_title",
                "courses_uuid"
            ],
            "ORDER": "courses_uuid"
        }
    },
    "isQueryValid": false,
    "result": "InsightError"
}
